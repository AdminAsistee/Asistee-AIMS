<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class ChannelManagerController extends Controller
{
    
    // public function performUpdate() {
    // 	$locations = Location::all();
    // 	$locations->each(function(Location $location) {
    // 		$GlobalLocationBookings = []; // array of date ranges
    // 		$listings = $location->listings->all();
    // 		$listings->each(function(Listing $listing) {
    // 			$Channel = AbstractChannel::getChannelObject($listing->channel_account->channel_id);

    // 		});
    // 	});
    // }
}
